public enum State
{
    playing,
    dialog,
    menu
}
public enum MovementState
{
    running,
    walking,
    standing,
}

public enum ShootingState
{
    shooting,
    reloading,
    aiming,
    noShoot
}